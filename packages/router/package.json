{
  "name": "@bifrost/routing",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "echo 'routing tests pending'"
  },
  "dependencies": {
    "@bifrost/shared": "workspace:*",
    "@bifrost/models": "workspace:*",
    "@bifrost/providers": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
